/**
 * Enum containing svg sources for all custom icons drawn from svg files
 * viewBox will be interpreted as "0 0 24 24"
 */
export enum CustomIcon {
    DRAWN_CROSS = `assets/img/drawn_cross.svg#group-R5`
}

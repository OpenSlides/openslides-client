import { TrustPipe } from './trust.pipe';

xdescribe(`TrustPipe`, () => {
    it(`create an instance`, () => {
        // const pipe = new TrustPipe();
        // expect(pipe).toBeTruthy();
    });
});

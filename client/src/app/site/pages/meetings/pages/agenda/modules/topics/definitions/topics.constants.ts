import { _ } from '@ngx-translate/core';

export const topicHeadersAndVerboseNames = {
    title: _(`Title`),
    text: _(`Text`),
    agenda_duration: _(`Duration in minutes`),
    agenda_comment: _(`Comment`),
    agenda_type: _(`Visibility on agenda`)
};

export class SpeakerAction {
    public static readonly CREATE = `speaker.create`;
    public static readonly UPDATE = `speaker.update`;
    public static readonly DELETE = `speaker.delete`;
    public static readonly START_SPEAK = `speaker.speak`;
    public static readonly PAUSE_SPEAK = `speaker.pause`;
    public static readonly UNPAUSE_SPEAK = `speaker.unpause`;
    public static readonly END_SPEAK = `speaker.end_speech`;
    public static readonly SORT_SPEAKERS = `speaker.sort`;
}

export class ChatMessageAction {
    public static readonly CREATE = `chat_message.create`;
    public static readonly UPDATE = `chat_message.update`;
    public static readonly DELETE = `chat_message.delete`;
}
